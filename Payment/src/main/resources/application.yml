spring:
  datasource:
    url: jdbc:mysql://localhost:3306/edumaster_payment
    username: {USERNAME}
    password: {USER_PASSWORD}
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
    hibernate:
      ddl-auto: update
server:
  port: 8087